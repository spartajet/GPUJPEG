# GPUJPEG Rust 重实现可行性评估（中文）

## 结论（先给结论）

可以使用 Rust 重新实现本项目的大部分功能，但建议采用**分阶段、可回退**的方式推进，而不是一次性全量替换。  
其中：

- **高可行**：CLI/参数解析、JPEG 码流解析与封装、CPU 侧控制流程、测试基础设施。
- **中等可行**：Huffman、量化表/采样因子处理、色彩空间转换（CPU 版）。
- **有条件可行**：GPU 核函数与 CUDA 互操作（取决于是否继续依赖 CUDA，或改为跨后端抽象）。

短期最稳妥路线是：**Rust 负责 host 侧与公共逻辑，CUDA 核函数先复用现有 C/CUDA 实现，通过 FFI 连接**；在性能与正确性稳定后，再逐步替换内核。

## 为什么可行

GPUJPEG 当前流程在 README 中已清晰分层（输入/预处理/DCT+量化/Huffman/格式化；解码反向流程），天然适合按模块迁移。  
Rust 在以下方面有直接收益：

1. 内存安全与并发安全（减少野指针、越界、生命周期错误）。
2. 更强的错误建模（`Result`/`thiserror`）有利于复杂参数与码流分支处理。
3. 可以保留 C ABI（`extern "C"`）与现有生态兼容，降低替换风险。

## 推荐总体方案

采用“三层架构”：

1. **core-rs（纯 Rust 业务层）**
   - JPEG 元数据结构、marker 解析、参数校验、任务编排。
2. **gpu-backend（后端抽象层）**
   - 定义统一 trait（如 `DctQuantBackend`、`HuffmanBackend`）。
   - 首版后端：`cuda-ffi-backend`（调用现有 `.cu/.c`）。
3. **ffi-compat（兼容层）**
   - 对外保留与 `libgpujpeg` 接近的 C API，保证已有调用方可渐进迁移。

## 迁移阶段（详细）

### Phase 0：基线与守护（1~2 周）

- 固化现有 C/CUDA 版本的正确性基线（编码-解码一致性、PSNR、性能）。
- 补齐最关键回归样例集合（不同色彩空间、采样、重启间隔）。
- 定义 Rust 版本验收阈值（见“验收标准”）。

### Phase 1：Rust CLI + 容器层（2~4 周）

- 用 Rust 重写命令行与参数层（可保留原工具命令语义）。
- 通过 FFI 直接调用现有 `libgpujpeg` 编解码入口。
- 目标：功能等价、用户无感切换、不触碰核心 CUDA 路径。

### Phase 2：码流与控制面迁移（4~8 周）

- Rust 实现 JPEG marker 读写、header 组装、参数派生逻辑。
- CPU 侧 Huffman（非 restart 并行路径）可先行迁移并对比输出比特级一致性。
- 保留 GPU 计算密集路径在原生 CUDA 中，降低性能回退风险。

### Phase 3：GPU 路径渐进替换（6~12 周）

- 先将 Rust 作为调度层，调用现有 CUDA kernel。
- 再按模块替换：预处理/后处理 -> DCT/IDCT -> 量化/反量化 -> restart 并行 Huffman。
- 每替换一块都做 A/B 性能与一致性对比，未达标可即时回退到旧后端。

### Phase 4：稳定化与发布（2~4 周）

- 完成跨平台打包、API 文档、升级指南。
- 形成 LTS 维护策略（C 兼容层支持周期、Rust 主线周期）。

## 关键技术选型建议

- **FFI/ABI**：`cbindgen` + `extern "C"`（导出 C 头，保持现有集成方兼容）。
- **错误处理**：`thiserror`/`anyhow`（库层/应用层分离）。
- **二进制解析**：优先手写 + 边界检查；必要时再用 `nom`。
- **并发**：`rayon` 仅用于 CPU 可并行阶段；GPU 调度保持可控线程模型。
- **CUDA 互操作**：优先复用既有 `.cu` 目标并通过 FFI 调用，避免首期引入过多不确定性。

## 风险与应对

1. **性能回退风险（最高）**
   - 应对：所有阶段都保留旧后端旁路；以真实 4K/8K/16K 基准做门禁。
2. **码流兼容性风险**
   - 应对：比特级对比 + 交叉解码（libjpeg-turbo/现 GPUJPEG 双向验证）。
3. **GPU 后端复杂度风险**
   - 应对：先“Rust 控制 + CUDA 计算”混合架构，延后内核重写。
4. **生态维护成本风险**
   - 应对：保持 C API 稳定，内部渐进替换，减少下游一次性迁移成本。

## 验收标准（建议）

- 正确性：核心测试集与现版本输出一致（允许元数据中非关键字段差异需可解释）。
- 兼容性：可解码现有 GPUJPEG 生成文件，且主要输入格式行为一致。
- 性能：
  - 编码/解码吞吐不低于现版本 **95%**（首个可发布目标）。
  - 热路径优化后追平或超过现版本。
- 可维护性：核心模块具备清晰边界与最小必要 unsafe。

## 人力与排期建议

- 建议 2~3 人小组：
  - 1 人负责 JPEG bitstream/兼容性；
  - 1 人负责 GPU/FFI 与性能；
  - 1 人负责测试基建与发布（可兼职）。
- 首个“可用但不全替换”的里程碑约 **2~3 个月**；全量替换视 GPU 内核重写深度约 **4~8 个月**。

## 最小可行落地路径（MVP）

如果希望快速验证方向，建议先做：

1. Rust CLI（参数保持兼容）；
2. Rust 封装现有 `libgpujpeg`；
3. 新增一致性测试与基准脚本；
4. 发布一个“Rust 前端 + 现有 CUDA 后端”的实验版本。

该路径风险最低，且最能验证“是否值得继续全量 Rust 化”。
